#include <stdio.h>

/**
 * main - Entry point, print alphabets in lowercase using putchar function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphabet = 'a';

	while (a <= 'z')
	{
		putchar(alphabet);
		alphabet++;
	}
	putchar("\n");

	return (0);
}
